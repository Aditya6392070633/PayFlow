import React,{useState}from'react';import{View,Text,TextInput,Button,FlatList,TouchableOpacity,StyleSheet}from'react-native';import{api}from'../api/client';const s=StyleSheet.create({p:{flex:1,padding:18,gap:12},card:{padding:16,borderWidth:1,borderColor:'#ddd',borderRadius:14,marginBottom:10},title:{fontSize:26,fontWeight:'800'},input:{borderWidth:1,borderColor:'#ccc',padding:12,borderRadius:10}});
export default function Auth(){const[phone,setPhone]=useState('');const[otp,setOtp]=useState('');async function send(){await api('/auth/send-otp',{method:'POST',body:JSON.stringify({phone})});alert('OTP sent: 123456')}async function verify(){const r=await api('/auth/verify-otp',{method:'POST',body:JSON.stringify({phone,otp,name:'PayFlow User'})});alert('Login success copy token from API in real app')}return <View style={s.p}><Text style={s.title}>PayFlow Login</Text><TextInput style={s.input} placeholder='Phone' value={phone} onChangeText={setPhone}/><Button title='Send OTP' onPress={send}/><TextInput style={s.input} placeholder='OTP' value={otp} onChangeText={setOtp}/><Button title='Verify OTP' onPress={verify}/></View>}
