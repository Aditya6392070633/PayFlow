import React,{useState}from'react';import{View,Text,TextInput,Button,FlatList,TouchableOpacity,StyleSheet}from'react-native';import{api}from'../api/client';const s=StyleSheet.create({p:{flex:1,padding:18,gap:12},card:{padding:16,borderWidth:1,borderColor:'#ddd',borderRadius:14,marginBottom:10},title:{fontSize:26,fontWeight:'800'},input:{borderWidth:1,borderColor:'#ccc',padding:12,borderRadius:10}});
export default function Send(){const[receiverPhone,setR]=useState('');const[amount,setA]=useState('');async function go(){await api('/transactions/send',{method:'POST',body:JSON.stringify({receiverPhone,amount:Number(amount)*100,note:'PayFlow transfer'})});alert('Sent successfully')}return <View style={s.p}><Text style={s.title}>Send Money</Text><TextInput style={s.input} placeholder='Receiver phone' value={receiverPhone} onChangeText={setR}/><TextInput style={s.input} placeholder='Amount ₹' value={amount} onChangeText={setA}/><Button title='Confirm & Send' onPress={go}/></View>}
